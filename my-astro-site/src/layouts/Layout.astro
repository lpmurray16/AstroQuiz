---
const { title } = Astro.props;
---

<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{title}</title>
  <meta name="description" content="A simple Quiz Application made with Astro and ReactJS">
  <link rel="icon" type="image/x-icon" href="/assets/harry-potter.ico">

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Ubuntu&display=swap" rel="stylesheet">

</head>

<body>
  <slot />
</body>

</html>

<style lang="scss">
  :root {
    --font-size-base: clamp(1rem, 0.34vw + 0.91rem, 1.19rem);
    --font-size-lg: clamp(1.2rem, 0.7vw + 1.2rem, 1.5rem);
    --font-size-xl: clamp(2.44rem, 2.38vw + 1.85rem, 3.75rem);

    --color-text: hsl(12, 5%, 4%);
    --color-bg: hsl(60, 100%, 97%);
    --color-border: hsl(60, 100%, 97%);
    --font-family: 'Ubuntu', sans-serif;
    --primary-color: rgb(93, 93, 238);
  }

  @media (prefers-color-scheme: dark) {
    :root {
      --color-text: hsl(0, 0%, 100%);
      --color-bg: hsl(12, 5%, 4%);
      --color-border: hsl(12, 5%, 4%);
    }
  }

  * {
    margin: 0;
    padding: 0;
    font-family: var(--font-family);
  }

  body {
    background-color: var(--color-bg);
    color: var(--color-text);
    font-family: var(--font-family);
    font-size: var(--font-size-base);
    line-height: 1.5;
    margin: 0;
    padding: 0;
    min-height: 100vh;
  }

  h1 {
    font-size: var(--font-size-xl);
    font-weight: bold;
    color: var(--color-text);
    margin: 0;
    padding: 0;
  }

  h2 {
    font-size: var(--font-size-lg);
    font-weight: bold;
    color: var(--color-text);
    margin: 0;
    padding: 0;
  }
</style>